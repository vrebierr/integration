nav.navbar.navbar-inverse.navbar-static-top(role='navigation')
	.container
		.navbar-header
			a.navbar-brand(ui-sref='admin') Administration

map#map_fullscreen(center='{{map.center}}', zoom='{{map.zoom}}', on-click='setMarker(event)')
	marker(ng-repeat='event in events', position='{{event.coords}}')

.container
	.row
		.col-md-6
		.col-md-6
			form(role='form')
				.form-group
					label Description
					textarea.fomr-control(ng-model='event.description', rows='15')
				button.btn.btn-default(ng-click='save()') Sauvegarder